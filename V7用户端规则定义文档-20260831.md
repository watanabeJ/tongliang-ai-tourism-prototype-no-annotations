# V7 用户端规则定义文档

## 1. 文档信息

- 产品：周末到铜梁 AI 文旅智能体（用户端）
- 版本：V7
- 日期：2026-08-31
- 规则状态：已确认规则可作为产品设计与验收依据；“待确认”事项不进入当前实现
- 适用范围：用户端首页、左侧历史对话抽屉、共享 AI 对话、心愿单、地图、推荐卡片和商品/商户详情
- 关联记录：`V7用户端迭代记录-20260831.md`
- 说明：本文件仅记录规则，不代表本次已修改原型代码；本次未更新产品说明书

## 2. 规则优先级

1. 用户最新明确输入优先于当前已确认意图。
2. 当前已确认意图优先于历史上下文中的旧意图。
3. 可靠的关联知识和实时数据优先于 AI 推测。
4. 无可靠数据时只展示待确认状态，不伪造价格、库存、房态、场次、距离或营业状态。
5. 页面展示规则、操作规则、数据留存规则和状态迁移规则分别执行，不以视觉表现替代业务规则。

## 3. 左侧抽屉与历史对话

### 3.1 留存与展示窗口

- 用户端默认展示最近 6 个月（180 天）内的历史对话。
- 超过 180 天的记录不在左侧抽屉展示；历史数据可由后台异步清理，不要求前端即时物理删除。
- 历史记录按中国标准时间（UTC+8）判断日期。

### 3.2 分组和排序

- 第一组固定为`今天`。
- 第二组固定为`昨天`。
- 之后按具体日期倒序展示，最近日期在前。
- 同一天内按最近更新时间倒序展示。
- 没有记录的日期不生成空分组。
- 超过 7 天不切换为月份分组，仍按具体日期连续展示；月份折叠属于后续优化项。

### 3.3 历史操作

- 点击历史记录：恢复该对话及其消息上下文。
- 新开对话：保存当前非空对话，再创建新的当前对话。
- 删除单条历史：只删除该条历史及其消息，不影响心愿单。
- 清空当前对话：只清空当前活动对话消息和临时计划，不删除其他历史记录，不清空心愿单。
- 当前对话为空时，不创建空历史记录。

### 3.4 已确认边界

- 超过 6 个月的历史对话完全不允许用户查看，不提供历史搜索入口。
- 历史对话只保存在当前设备，不做跨设备同步。

## 4. 中英文切换

### 4.1 切换范围

- 语言切换同时影响页面固定文案和切换后的新 AI 回复。
- 页面固定文案包括导航、按钮、状态、空状态、错误提示、操作反馈和帮助性提示。
- 已经显示的历史 AI 消息不因切换语言而重新翻译。
- 切换语言不改变对话、心愿单、定位和页面路由状态。

### 4.2 AI 回复语言

- AI 默认按当前页面语言回复。
- 用户可以使用中文或英文输入；系统按当前页面语言组织回复。
- 用户明确要求使用另一种语言时，可临时按用户要求回复，但不自动改变全局语言设置。

### 4.3 专有名词

- 景点名、商户名、商品名、活动名和地址默认保留中文原名。
- 英文界面可在中文原名后增加英文解释或类别说明，不强制创造未经确认的官方英文名。
- 价格、电话、日期和地址中的数字及格式不因语言切换而改变业务含义。

### 4.4 语言偏好

- 当前语言偏好只在本次用户会话内保持。
- 用户每次重新进入产品时，语言恢复为中文。
- 语言偏好不跨设备、跨登录会话长期保存。

## 5. 心愿单与心愿地图

### 5.1 心愿地图激活条件

- 必须勾选至少 2 条有效心愿记录，才显示`心愿地图`入口。
- 只勾选 1 条时不显示心愿地图；用户可进入该条详情或使用推荐卡片定位入口。
- 已勾选数量从 2 条降到 1 条或 0 条时，心愿地图入口立即隐藏。

### 5.2 勾选状态

- 心愿记录本身可跨页面保留。
- 勾选状态只在当前用户会话内保留；关闭当前会话或重新开始会话后清除勾选状态。
- 删除或清空心愿记录时同步移除对应勾选状态。
- 心愿地图只读取仍有效且当前被勾选的记录。
- 用户在心愿地图的 POI 卡片中取消加入心愿单后，系统立即移除当前点位及关联路线；页面刷新、再次进入心愿地图或使用旧的已选集合时，均不得再展示该已移除点位。

### 5.3 无坐标记录

- 没有可靠经纬度的商品、商户或内容仍可以加入心愿单。
- 无可靠坐标的记录不进入心愿地图，并在需要时标记“暂无法定位”。
- 不使用未经确认的地址推算精确坐标。

### 5.4 心愿单清空

- `清空心愿单`属于高影响操作，必须二次确认。
- 确认后删除全部心愿记录并清除全部勾选状态。
- 取消确认不改变任何记录。

## 6. 对话面板与消息操作

### 6.1 清空当前对话

- 右上角扫帚图标的名称为`清空当前对话记录`。
- 清空范围：当前活动对话消息、当前地图对话消息和当前临时计划状态。
- 不清空：其他历史对话、心愿单记录、语言偏好和已确认的运营主题。
- 当前存在消息时，点击清空先弹出二次确认；确认后执行清空。
- 当前没有消息时，点击清空不弹窗，并提示“当前没有可清空的对话”。

### 6.2 默认状态

- 首次打开对话面板不展示“我是铜梁小小龙，随时为您服务”。
- 空对话不展示“我们边走边耍，您想怎么耍，随时告诉我”。
- 清空后回到同样的空对话状态，不自动插入欢迎消息。

### 6.3 AI 消息操作

- 每条 AI 回复底部保留复制、喜欢、不喜欢。
- 长按或右键 AI 回复不再弹出操作菜单。
- 不提供通过长按菜单删除 AI 消息、分享 AI 消息或收藏 AI 消息的入口。
- AI 回复末尾显示“内容由 AI 生成，仅供参考”。

### 6.4 AI 异常与计划边界

- AI 回复失败消息保存到历史对话，方便用户查看当时发生过什么。
- 失败消息必须标记为失败状态，不得被当作成功回答或有效推荐结果。
- 用户端没有“加入计划”功能，AI 不得自动创建、加入或修改计划；用户只可使用心愿单保存感兴趣的商品或商户。

## 7. 跑马灯与轮播主图追问

### 7.1 跑马灯意图卡片

- 所有跑马灯意图卡片统一采用“问题 + 回复提示”版式。
- 不展示卡片标题。
- 不展示“查看系统提示词与关联知识”折叠区。
- 不展示快捷选项按钮。
- 回复提示应说明用户可以直接输入对应需求；不同主题的选项名称可合并到提示文字中。
- 用户输入后，由当前意图规则决定直接返回结果或继续必要补问；AI 最多连续追问 10 次。
- AI 无法识别或用户需求超出平台服务范围时，显示：“您的需求超出平台提供的服务范围，平台只提供铜梁区域范围内的吃、住、游、购、娱相关商户和商品信息以及相关问答回复，非常抱歉不能帮助到您。”

### 7.2 轮播主图追问

- 玄天湖、周末和玄天湖骑行轮播主图使用统一追问：

  `骑行后你想优先解决什么？继续徒步，吃一顿饭，还是，住一晚或买伴手礼？`

- 三个追问选项不以按钮形式展示。
- 用户通过底部输入框自由回复。
- 轮播主图的追问不改变已展示的知识推荐和实体卡片。

## 8. 定位与地图

- 首次进入首页不主动申请定位。
- 用户进入地图或主动使用附近服务时，说明用途并申请一次当前位置。
- 一次定位结果只用于当前地图、附近服务和当前浏览上下文，不开启持续定位。
- 用户拒绝定位时，普通地图固定 POI 仍可浏览；附近服务不得生成伪造的附近结果或精确距离。
- 用户再次主动使用需要定位的功能时，可以重新申请一次定位。
- 推荐卡片中的蓝色 `pin` 图标用于查看对应 POI；详情页不显示地图图标入口。
- 心愿地图不以当前定位作为展示已选心愿 POI 的前置条件。

## 8.1 运营主题失效与加载失败

- 运营主题过期或加载失败时，加载最近一次有效主题。
- 最近一次有效主题作为整体快照恢复，背景底图、游玩灵感、主题标语和轮播主图不得跨版本拼接。
- 没有可用的历史主题快照时，才进入产品默认主题或对应空状态。

## 9. 推荐卡片与详情页

### 9.1 推荐卡片

- 商品和商户推荐卡片右下角只保留定位和加入心愿单两个图标。
- 定位图标使用蓝色 `pin`，尺寸与心愿单图标一致。
- 点击卡片主体进入商品/商户详情；点击定位图标进入应用内地图；点击心愿单图标加入心愿单。
- 重复加入同一对象不得产生重复记录。

### 9.2 详情页信息

- 所有商品和商户详情页移除推荐商品/套餐模块。
- 所有详情页移除地图图标入口。
- 详情信息区不展示营业时间与人均价格摘要行。
- 地址以文本展示，电话在有可靠号码时提供拨号入口。
- 详情页提供居中的紫色胶囊按钮`进入店铺查看详情`。
- 点击店铺按钮只显示“店铺详情已打开”反馈，不跳转商户后台。
- 玄天湖湖景火锅的商户摘要名称为`玄天湖湖景火锅（玄天湖店）`；页面主标题保持`玄天湖湖景火锅`。
- 当前详情页不提供可达的购买、支付或住宿预订流程。
- 商户下架后，历史消息中的原卡片保留，但禁止进入详情、地图和心愿单操作，并明确标记内容已下架或已失效。

### 9.3 数据不完整时

- 营业状态、价格、库存、房态、场次或距离缺失时显示待确认信息。
- 数据不完整时只允许查看、收藏或其他已有非交易操作。
- 不允许使用 AI 估算值替代正式业务数据。

## 10. 后续议题

- 本轮已列出的待确认事项均已完成确认并写入本文件。
- 后续新增规则议题需单独确认后再追加，不默认改变本文件已有规则。
